<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#fff"
    android:orientation="vertical" >

    <!-- <ScrollView -->
    <!-- android:layout_width="match_parent" -->
    <!-- android:layout_height="wrap_content" > -->


    <!-- <LinearLayout -->
    <!-- android:layout_width="match_parent" -->
    <!-- android:layout_height="wrap_content" -->
    <!-- android:orientation="vertical" > -->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical" >

        <ImageView
            android:layout_width="@dimen/login_logo_width"
            android:layout_height="@dimen/login_logo_height"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="40dp"
            android:background="@drawable/ic_launcher" />

        <EditText
            android:id="@+id/edt_username"
            style="@style/common_edtview"
            android:layout_marginTop="40dp"
            android:drawableLeft="@drawable/user_icon"
            android:hint="请输入用户名"
            android:inputType="text" />

        <EditText
            android:id="@+id/edt_userpwd"
            style="@style/common_edtview"
            android:drawableLeft="@drawable/user_pwd"
            android:hint="请输入密码"
            android:inputType="textPassword" />

        
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginLeft="@dimen/login_edt_padding"
            android:layout_marginRight="@dimen/login_edt_padding"
            android:layout_marginTop="15dp" >

            <CheckBox
                android:id="@+id/ck_rem_pwd"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentLeft="true"
                android:layout_centerVertical="true"
                android:button="@null"
                android:drawableLeft="@drawable/login_ck_enable"
                android:drawablePadding="2dp"
                android:paddingLeft="0dp"
                android:text="记住密码"
                android:textColor="@drawable/login_checkbox_textcolor_selector" />

        </RelativeLayout>

        <TextView
            android:id="@+id/login_btn"
            style="@style/common_btn"
            android:layout_marginLeft="@dimen/login_edt_padding"
            android:layout_marginRight="@dimen/login_edt_padding"
            android:layout_marginTop="15dp"
            android:background="@drawable/blue_btn_selector"
            android:text="登录" />

    </LinearLayout>


    <!-- </LinearLayout> -->
    <!-- </ScrollView> -->

</LinearLayout>